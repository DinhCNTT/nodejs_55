export const userService = {
    async avatarLocal(req) {
        return `This action upload avatar local`;
    },

    async avatarCloud(req) {
        return `This action upload avatar cloud`;
    },

};